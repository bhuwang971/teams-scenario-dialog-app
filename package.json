{
  "name": "teams-scenario-dialog-app",
  "version": "1.0.0",
  "private": true,
  "description": "Standard Teams tab app for scenario analysis dialog",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "zip:app": "powershell -NoProfile -Command \"New-Item -ItemType Directory -Force appPackage/build | Out-Null; Compress-Archive -Path appPackage/manifest.json,appPackage/color.png,appPackage/outline.png -DestinationPath appPackage/build/appPackage.zip -Force\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
